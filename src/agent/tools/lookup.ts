// Owned by W1-A (see docs/briefs/). Register tools here; the registry merges this map.
import type { ToolDef } from "@/agent/registry";

export const tools: Record<string, ToolDef> = {
  // find_address
  // find_customer
  // save_caller_phone
};
