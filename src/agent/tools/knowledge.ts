// Owned by W1-C (see docs/briefs/). Register tools here; the registry merges this map.
import type { ToolDef } from "@/agent/registry";

export const tools: Record<string, ToolDef> = {
  // get_address_dossier
  // get_visit_history
  // get_job_notes
  // get_job
  // check_warranty
  // get_open_balance
  // get_schedule
};
